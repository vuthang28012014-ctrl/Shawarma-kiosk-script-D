loadstring(game:HttpGet("https://raw.githubusercontent.com/vuthang28012014-ctrl/Shawarma-kiosk-script-D/refs/heads/main/UI%20Load%20script"))()
wait(0.5)
loadstring(game:HttpGet("https://raw.githubusercontent.com/vuthang28012014-ctrl/Shawarma-kiosk-script-D/refs/heads/main/Welcome%20user"))()
wait(2)
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")

local player = Players.LocalPlayer
local config = loadstring(game:HttpGet("https://raw.githubusercontent.com/vuthang28012014-ctrl/Shawarma-kiosk-script-D/refs/heads/main/config.lua"))()
local webhook = config.webhook
local executor = "Unknown"

if identifyexecutor then
    executor = identifyexecutor()
elseif getexecutorname then
    executor = getexecutorname()
end

local data = {
    ["content"] = "",
    ["embeds"] = {{
        ["title"] = "Script Execution Log",
        ["color"] = 65280,
        ["fields"] = {
            {
                ["name"] = "Username",
                ["value"] = player.Name,
                ["inline"] = true
            },
            {
                ["name"] = "UserId",
                ["value"] = tostring(player.UserId),
                ["inline"] = true
            },
            {
                ["name"] = "Executor",
                ["value"] = executor,
                ["inline"] = true
            },
            {
                ["name"] = "PlaceId",
                ["value"] = tostring(game.PlaceId),
                ["inline"] = true
            }
        }
    }}
}

local json = HttpService:JSONEncode(data)

request({
    Url = webhook,
    Method = "POST",
    Headers = {
        ["Content-Type"] = "application/json"
    },
    Body = json
})
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")

local player = Players.LocalPlayer

local url = "https://raw.githubusercontent.com/vuthang28012014-ctrl/Ban-list---AppleHub/main/Banlist.json"

local data = game:HttpGet(url)
local bans = HttpService:JSONDecode(data)

local id = tostring(player.UserId)

print("My ID:", id)
print("Check ban:", bans[id])

local ContentToKick = "Kicked because script is currently down (outage)"
local Kick = false -- (IMPORTANT): CHANGE TO TRUE IF WANT TO KICK
if executor == "Xeno" then
Kick = true
ContentToKick = "You were kicked because your executor is not supported!"
end
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
Rayfield:Notify({
   Title = "Loading!",
   Content = "Script made by vuthang28012014",
   Duration = 6.5,
   Image = 4483362458,
})
if bans[id] == true then
    print("Kicked")
    player:Kick("You has been banned by our owner!")
end
if Kick == true then
    Rayfield:Notify({
        Title = "Script Error",
        Content = ContentToKick,
        Duration = 4
    })

    task.wait()

    game.Players.LocalPlayer:Kick(ContentToKick)
else
    print("Kick system disabled")
end
local Window = Rayfield:CreateWindow({
   Name = "Shawarma kiosk exploit made by vuthangtuyenquang",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "Loading...",
   LoadingSubtitle = "by vuthangtuyenquang",
   ShowText = "Shawarma kiosk exploit", -- for mobile users to unhide rayfield, change if you'd like
   Theme = "Default", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   ToggleUIKeybind = "K", -- The keybind to toggle the UI visibility (string like "K" or Enum.KeyCode)

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = false,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "nil"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
      RememberJoins = false -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = true, -- Set this to true to use our key system
   KeySettings = {
      Title = "Shawarma kiosk key system",
      Subtitle = "Key System",
      Note = "Get key here: https://discord.gg/SDj4QwYE", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"vuthangdeptrai"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})
if Kick == true then
    Rayfield:Destroy()
end
local Tab = Window:CreateTab("Farm", 4483362458) -- Title, Image
local Section = Tab:CreateSection("Farm")
Section:Set("Farm")
local AutoFarm = false

local stations = {
    Vector3.new(-47,5,-555), -- refill meat
    Vector3.new(-48,5,-550), -- lavash
    Vector3.new(-44,5,-554), -- soda
    Vector3.new(-42,5,-548)  -- make / give
}

local allowed = {
    ["take"] = true,
    ["put"] = true,
    ["roll"] = true,
    ["give"] = true,
    ["give soda"] = true,
    ["restock"] = true
}

local function interactNearby()
    for _,v in pairs(workspace:GetDescendants()) do
        if v:IsA("ProximityPrompt") and v.Enabled then
            
            local action = (v.ActionText or ""):lower()
            
            if allowed[action] then
                fireproximityprompt(v)
                task.wait(0.4)
            end
        end
    end
end

local Toggle = Tab:CreateToggle({
   Name = "Autofarm v1",
   CurrentValue = false,
   Flag = "autofarmv1",

   Callback = function(Value)
        AutoFarm = Value

        if Value then
            task.spawn(function()

                local player = game.Players.LocalPlayer
                local root = player.Character.HumanoidRootPart

                while AutoFarm do
                    for _,pos in ipairs(stations) do
                        if not AutoFarm then break end
                        
                        root.CFrame = CFrame.new(pos)
                        task.wait(0.25)

                        interactNearby()
                    end
                    
                    task.wait(0.2)
                end

            end)
        end
   end,
})
local Tab = Window:CreateTab("Some boost", 4483362458) -- Title, Image
local Section = Tab:CreateSection("Some boost")
Section:Set("Some boost")
Section:Set("Some boost")
local Divider = Tab:CreateDivider()
Divider:Set(true) -- Whether the divider's visibility is to be set to true or false.
-- Boost name mapping
local Boosts = {
    ["Meat Ticket"] = "MeatTicket",
    ["2x Earn"] = "2xEarn",
    ["Scare a Friend"] = "ScreamPlayer",
    ["Dance for Friends"] = "Scream",
    ["Show Anomaly"] = "ShowAnomaly",
    ["Summon Anomaly"] = "SummonAnomaly",
    ["Internal Control"] = "InternalC",
    ["Tips"] = "Tips",
    ["Corruption Deal"] = "Ticket"
}

-- Dropdown display list
local Options = {}
for name,_ in pairs(Boosts) do
    table.insert(Options,name)
end

-- DROPDOWN MENU
local Dropdown = Tab:CreateDropdown({
   Name = "Boost Menu",
   Options = Options,
   CurrentOption = {"Meat Ticket"},
   MultipleOptions = false,
   Flag = "BoostDropdown",

   Callback = function(Selected)
       local displayName = Selected[1]
       local boostName = Boosts[displayName]

       game:GetService("ReplicatedStorage")
           :WaitForChild("UseBoost")
           :FireServer(boostName)
   end,
})
local Section = Tab:CreateSection("Skip tutorial")
Section:Set("Skip tutorial")
local Button = Tab:CreateButton({
   Name = "Skip the tutorial",
   Callback = function()
   game:GetService("ReplicatedStorage"):WaitForChild("SkipTutorial"):FireServer()
   Rayfield:Notify({
   Title = "Running code!",
   Content = "Skipping the tutorial...",
   Duration = 6.5,
   Image = 4483362458,
})
   end,
})
Button:Set("Skip the tutorial")
local Section = Tab:CreateSection("Buying boost")
Section:Set("Buying boost")
-- Buy boost mapping
local BuyBoosts = {
    ["Internal Control"] = "internalc",
    ["Dance For Friends"] = "Scream",
    ["Scare A Friend"] = "ScreamPlayer",
    ["Corruption Deal"] = "corruption",
    ["Show Anomaly"] = "show",
    ["Summon Anomaly"] = "summon"
}

-- Dropdown options
local BuyOptions = {}
for name,_ in pairs(BuyBoosts) do
    table.insert(BuyOptions, name)
end

-- DROPDOWN MENU
local BuyDropdown = Tab:CreateDropdown({
   Name = "Buy Boost",
   Options = BuyOptions,
   CurrentOption = {},
   MultipleOptions = false,
   Flag = "BuyBoostDropdown",

   Callback = function(Selected)
       local displayName = Selected[1]
       local actionName = BuyBoosts[displayName]

       local args = {
           {
               action = actionName
           }
       }

       game:GetService("ReplicatedStorage")
           :WaitForChild("ShopCurrencyBuy")
           :FireServer(unpack(args))
   end,
})
local Tab = Window:CreateTab("Some Badges", 4483362458) -- Title, Image
local Section = Tab:CreateSection("Some Badges")
Section:Set("Some Badges")
-- Badge ID mapping
local BadgeMap = {
    ["Perfect shift"] = 2335580643689682,
    ["The Inspector"] = 675811825688661,
    ["Unstable shift"] = 1548943367795853,
    ["Open the door"] = 3446636560307654,
    ["Obedience"] = 1996341215013965,
    ["Heartless"] = 792904025880360,
    ["Accepted by the deep"] = 3431327816967957,
    ["Yes I am a human"] = 3925239037360973,
    ["The Other Reality"] = 2310543252637783,
    ["Erased from the reality"] = 3859530300923014
}

-- Dropdown menu
local Dropdown = Tab:CreateDropdown({
   Name = "Select Badge",
   Options = {
       "Perfect shift",
       "The Inspector",
       "Unstable shift",
       "Open the door",
       "Obedience",
       "Heartless",
       "Accepted by the deep",
       "Yes I am a human",
       "The Other Reality",
       "Erased from the reality"
   },
   CurrentOption = {"Perfect shift"},
   MultipleOptions = false,
   Flag = "BadgeDropdown",

   Callback = function(Options)
       local selected = Options[1] -- since MultipleOptions = false
       local badgeId = BadgeMap[selected]

       if badgeId then
           local args = {badgeId}
           game:GetService("ReplicatedStorage")
               :WaitForChild("RequestBadge")
               :FireServer(unpack(args))
       else
           warn("Invalid selection:", selected)
       end
   end,
})
local Tab = Window:CreateTab("Teleport", 4483362458) -- Title, Image
local Section = Tab:CreateSection("Teleport")
Section:Set("Teleport")
local Button = Tab:CreateButton({
   Name = "Teleport to the bus stop",
   Callback = function()
   Rayfield:Notify({
   Title = "Teleporting!",
   Content = "Teleporting to the bus stop...",
   Duration = 6.5,
   Image = 4483362458,
})
   game:GetService("ReplicatedStorage"):WaitForChild("RestoreMorph"):FireServer()
   end,
})
Button:Set("Teleport to the bus stop")
local Button = Tab:CreateButton({
   Name = "Teleport to the dial",
   Callback = function()
   Rayfield:Notify({
   Title = "Teleporting!",
   Content = "Teleporting to the dial...",
   Duration = 6.5,
   Image = 4483362458,
})
   local args = {
	true
}
game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("HandleCallEvent"):FireServer(unpack(args))
game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("HandleCallEvent"):FireServer()
  end,
})
Button:Set("Teleport to the dial")
local Tab = Window:CreateTab("Menu delivery", 4483362458) -- Title, Image
local Button = Tab:CreateButton({
   Name = "Open the menu",
   Callback = function()
   Rayfield:Notify({
   Title = "Openning!",
   Content = "Openning the menu...",
   Duration = 6.5,
   Image = 4483362458,
})
local args = {
	true
}
game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("HandleCallEvent"):FireServer(unpack(args))
game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("OpenDeliveryMenuEvent"):FireServer()
  end,
})
Button:Set("Open the menu")
local Section = Tab:CreateSection("Order")
Section:Set("Order")
function doStuff()
    local options = getgenv().SelectedOption

    for _, option in pairs(options) do
        local args = {
            { option }
        }

        game:GetService("ReplicatedStorage")
            :WaitForChild("Remotes")
            :WaitForChild("ConfirmDeliveryOrderEvent")
            :FireServer(unpack(args))
    end
end
local Dropdown = Tab:CreateDropdown({
   Name = "Order menu",
   Options = {"Bell", "Decorations", "Camera", "Kebab"},
   CurrentOption = {"None"},
   MultipleOptions = true,
   Flag = "Dropdown1", -- A flag is the identifier for the configuration file; make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Options)
      getgenv().SelectedOption = Options
      doStuff()
   end,
})
local Tab = Window:CreateTab("Chat", 4483362458) -- Title, Image
local Section = Tab:CreateSection("Shawarma's Text chat service")
Section:Set("Shawarma's Text chat service")
local Input = Tab:CreateInput({
   Name = "Chat",
   CurrentValue = "",
   PlaceholderText = "Type here to chat",
   RemoveTextAfterFocusLost = true,
   Flag = "Input1",
   Callback = function(Text)
Rayfield:Notify({
   Title = "Sending text to the shawarma's text chat service",
   Content = "Sending", Text,
   Duration = 6.5,
   Image = 4483362458,
})
   local args = {
	Text
}
game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("QuickChatEvent"):FireServer(unpack(args))
   end,
})
local Tab = Window:CreateTab("LocalPlayer", 4483362458) -- Title, Image
local Section = Tab:CreateSection("FPS")
-- Brightness
local Thunder = game:GetService("Lighting").Thunder
local Brightness = Thunder.Brightness

-- Toggle
local Toggle = Tab:CreateToggle({
   Name = "White Screen",
   CurrentValue = false,
   Flag = "Brightness",
   Callback = function(Value)
      if Value then
         Thunder.Brightness = 1
      else
         Thunder.Brightness = 0
      end
   end,
})
local Section = Tab:CreateSection("WalkSpeed & JumpPower")
-- Player
local Players = game:GetService("Players")
local Player = Players.LocalPlayer

-- Slider
local Slider = Tab:CreateSlider({
   Name = "WalkSpeed",
   Range = {0, 1000},
   Increment = 1,
   Suffix = "Speed",
   CurrentValue = 6,
   Flag = "WalkSpeedSlider",

   Callback = function(Value)
      local Character = Player.Character or Player.CharacterAdded:Wait()
      local Humanoid = Character:FindFirstChildOfClass("Humanoid")

      if Humanoid then
         Humanoid.WalkSpeed = Value
      end
   end,
})

-- JumpPower Slider
local Slider = Tab:CreateSlider({
   Name = "JumpPower",
   Range = {0, 200},
   Increment = 5,
   Suffix = "Jump",
   CurrentValue = 50,
   Flag = "JumpPowerSlider",

   Callback = function(Value)
      local Character = Player.Character or Player.CharacterAdded:Wait()
      local Humanoid = Character:FindFirstChildOfClass("Humanoid")

      if Humanoid then
         Humanoid.JumpPower = Value
      end
   end,
})
-- Lighting
local Lighting = game:GetService("Lighting")

-- Brightness Slider
local Slider = Tab:CreateSlider({
   Name = "Brightness",
   Range = {0, 100}, -- change max if you want
   Increment = 0.1,
   Suffix = "Light",
   CurrentValue = Lighting.Brightness,
   Flag = "BrightnessSlider",

   Callback = function(Value)
      Lighting.Brightness = Value
   end,
})

-- Get player and RunService
local RunService = game:GetService("RunService")
local player = game:GetService("Players").LocalPlayer

local noclip = false

-- Toggle
local Toggle = Tab:CreateToggle({
   Name = "Noclip",
   CurrentValue = false,
   Flag = "NoclipToggle",

   Callback = function(Value)
      noclip = Value
   end,
})

-- Noclip
RunService.Stepped:Connect(function()
   if not noclip then return end

   local character = player.Character
   if not character then return end

   for _, part in ipairs(character:GetDescendants()) do
      if part:IsA("BasePart") then
         part.CanCollide = false
      end
   end
end)
