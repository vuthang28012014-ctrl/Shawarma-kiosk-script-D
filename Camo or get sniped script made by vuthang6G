local Players = game:GetService("Players")
local PlayerIsExecuting = Players.LocalPlayer.Name
local function LoadEnvironment()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/vuthang28012014-ctrl/Shawarma-kiosk-script-D/refs/heads/main/UI%20Load%20script"))()
	wait(0.5)
end
if PlayerIsExecuting ~= "Hackerissomuch_20" then
	LoadEnvironment()
end
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local MarketplaceService = game:GetService("MarketplaceService")

local player = Players.LocalPlayer
local config = loadstring(game:HttpGet("https://raw.githubusercontent.com/vuthang28012014-ctrl/Shawarma-kiosk-script-D/refs/heads/main/config.lua"))()
local webhook = config.webhook
local executor = "Unknown"

if identifyexecutor then
    executor = identifyexecutor()
elseif getexecutorname then
    executor = getexecutorname()
end

local gameName = "Unknown"
pcall(function()
    gameName = MarketplaceService:GetProductInfo(game.PlaceId).Name
end)

local data = {
    ["content"] = "",
    ["embeds"] = {{
        ["title"] = "Script Execution Log",
        ["color"] = 65280,
        ["fields"] = {
            {
                ["name"] = "Username",
                ["value"] = player.Name,
                ["inline"] = true
            },
            {
                ["name"] = "UserId",
                ["value"] = tostring(player.UserId),
                ["inline"] = true
            },
            {
                ["name"] = "Executor",
                ["value"] = executor,
                ["inline"] = true
            },
            {
                ["name"] = "PlaceId",
                ["value"] = tostring(game.PlaceId),
                ["inline"] = true
            },
			{
                ["name"] = "Game Name",
                ["value"] = gameName,
                ["inline"] = true
            }
        }
    }}
}

local json = HttpService:JSONEncode(data)

request({
    Url = webhook,
    Method = "POST",
    Headers = {
        ["Content-Type"] = "application/json"
    },
    Body = json
})
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")

local player = Players.LocalPlayer

local url = "https://raw.githubusercontent.com/vuthang28012014-ctrl/Ban-list---AppleHub/main/Banlist.json"

local data = game:HttpGet(url)
local bans = HttpService:JSONDecode(data)

local id = tostring(player.UserId)

print("My ID:", id)
print("Check ban:", bans[id])

local ContentToKick = "Kicked because script is currently down (outage)"
local Kick = false -- (IMPORTANT): CHANGE TO TRUE IF WANT TO KICK
if executor == "Xeno" then
Kick = true
ContentToKick = "You were kicked because your executor is not supported!"
end
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
Rayfield:Notify({
   Title = "Loading!",
   Content = "Script made by vuthang28012014",
   Duration = 6.5,
   Image = 132863234944847,
})
if bans[id] == true then
    print("Kicked")
    player:Kick("You has been banned by our owner!")
end
if Kick == true then
    Rayfield:Notify({
        Title = "Script Error",
        Content = ContentToKick,
        Duration = 4
    })

    task.wait()

    game.Players.LocalPlayer:Kick(ContentToKick)
else
    print("Kick system disabled")
end
local Window = Rayfield:CreateWindow({
   Name = "Camo or get sniped script",
   Icon = 132863234944847, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "Loading...",
   LoadingSubtitle = "by Hackerissomuch",
   ShowText = "Camo or get sniped script", -- for mobile users to unhide Rayfield, change if you'd like
   Theme = "Default", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   ToggleUIKeybind = "K", -- The keybind to toggle the UI visibility (string like "K" or Enum.KeyCode)

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from emitting warnings when the script has a version mismatch with the interface.


   ConfigurationSaving = {
      Enabled = false,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "Big Hub"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include Discord.gg/. E.g. Discord.gg/ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the Discord every time they load it up
   },

   KeySystem = false, -- Set this to true to use our key system
   KeySettings = {
      Title = "Camo or get sniped key system",
      Subtitle = "Key System",
      Note = "Get key here: https://discord.gg/SDj4QwYE", -- Use this to tell the user how to get a key
      FileName = "Key_2", -- It is recommended to use something unique, as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"vuthangdeptrai"} -- List of keys that the system will accept, can be RAW file links (pastebin, github, etc.) or simple strings ("hello", "key22")
   }
})
if Kick == true then
    Rayfield:Destroy()
end
local Tab = Window:CreateTab("Sniper", 4483362458) -- Title, Image
local Section = Tab:CreateSection("Kill")
local Players = game:GetService("Players")

local function getPlayerList()
    local list = {}
    
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= Players.LocalPlayer then
            table.insert(list, plr.Name)
        end
    end
    
    return list
end

local selectedPlayer = nil

local Dropdown = Tab:CreateDropdown({
   Name = "Select Player",
   Options = getPlayerList(),
   CurrentOption = {},
   MultipleOptions = false,
   Flag = "PlayerDropdown",
   Callback = function(Options)
       selectedPlayer = Options[1]
       local args = {
	   game:GetService("Players"):WaitForChild(selectedPlayer).Character 
       }
       game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_net@0.2.0"):WaitForChild("net"):WaitForChild("RE/ShootSniper"):FireServer(unpack(args))
   end,
})

-- Update when players changing
Players.PlayerAdded:Connect(function()
    Dropdown:Refresh(getPlayerList())
end)

Players.PlayerRemoving:Connect(function()
    Dropdown:Refresh(getPlayerList())
end)
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local remote = ReplicatedStorage
    :WaitForChild("Packages")
    :WaitForChild("_Index")
    :WaitForChild("sleitnick_net@0.2.0")
    :WaitForChild("net")
    :WaitForChild("RE/ShootSniper")

local Button = Tab:CreateButton({
   Name = "Kill all",
   Callback = function()
       
       task.spawn(function() -- prevent freeze UI
           
           for _, plr in ipairs(Players:GetPlayers()) do
               if plr ~= Players.LocalPlayer and plr.Character then
                   
                   local args = {plr.Character}
                   remote:FireServer(unpack(args))
                   
                   print("Sent to:", plr.Name)
                   
                   task.wait(1) -- delay 1s each player
               end
           end
           
       end)
       
   end,
})
local Section = Tab:CreateSection("Esp")
local Players = game:GetService("Players")

local espEnabled = false
local espObjects = {}

local function removeESP(plr)
    if espObjects[plr] then
        if espObjects[plr].highlight then
            espObjects[plr].highlight:Destroy()
        end
        if espObjects[plr].billboard then
            espObjects[plr].billboard:Destroy()
        end
        espObjects[plr] = nil
    end
end

local function createESP(plr)
    if plr == Players.LocalPlayer then return end

    local function setup(char)
        if not espEnabled then return end

        removeESP(plr)

        local humanoid = char:WaitForChild("Humanoid", 5)
        local head = char:WaitForChild("Head", 5)
        if not humanoid or not head then return end

        -- Highlight
        local highlight = Instance.new("Highlight")
        highlight.FillColor = Color3.fromRGB(255,255,255)
        highlight.OutlineColor = Color3.fromRGB(255,255,255)
        highlight.FillTransparency = 0.7
        highlight.Parent = char

        -- Billboard
        local billboard = Instance.new("BillboardGui")
        billboard.Size = UDim2.new(0, 110, 0, 25)
        billboard.StudsOffset = Vector3.new(0, 4.2, 0)
        billboard.AlwaysOnTop = true
        billboard.Parent = head

        local text = Instance.new("TextLabel")
        text.Size = UDim2.new(1,0,1,0)
        text.BackgroundTransparency = 1
        text.TextScaled = false
        text.TextSize = 13
        text.TextColor3 = Color3.fromRGB(255,255,255)
        text.TextStrokeTransparency = 0.3
        text.Font = Enum.Font.SourceSansBold
        text.Parent = billboard

        espObjects[plr] = {
            highlight = highlight,
            billboard = billboard
        }

        -- update nhẹ, không spam loop mạnh
        task.spawn(function()
            while espEnabled and plr.Parent and char.Parent do
                if humanoid then
                    text.Text = plr.Name .. " | HP: " .. math.floor(humanoid.Health)
                end
                task.wait(0.25)
            end
        end)
    end

    if plr.Character then
        setup(plr.Character)
    end

    plr.CharacterAdded:Connect(function(char)
        task.wait(0.5)
        setup(char)
    end)
end

local function clearAll()
    for plr,_ in pairs(espObjects) do
        removeESP(plr)
    end
end


local Toggle = Tab:CreateToggle({
   Name = "ESP",
   CurrentValue = false,
   Flag = "ESP_STABLE_FINAL",
   Callback = function(Value)
       espEnabled = Value

       if not Value then
           clearAll()
           return
       end

       for _, plr in ipairs(Players:GetPlayers()) do
           createESP(plr)
       end
   end,
})

Players.PlayerAdded:Connect(function(plr)
    createESP(plr)
end)

Players.PlayerRemoving:Connect(function(plr)
    removeESP(plr)
end)
local Section = Tab:CreateSection("Gun mods")
local Slider = Tab:CreateSlider({
   Name = "Set cooldown",
   Range = {0, 2},
   Increment = 1,
   Suffix = "Seconds",
   CurrentValue = 2,
   Flag = "Cooldown", -- A flag is the identifier for the configuration file; make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
   local debounce = require(game.ReplicatedStorage.Vendor.FnUtil.debounce)

hookfunction(debounce, function(fn)
    return function(...)
        return fn(...)
    end
end)
for _, v in pairs(getgc(true)) do
    if type(v) == "function" and islclosure(v) then
        local consts = debug.getconstants(v)

        if table.find(consts, "PerformSnipe")
        and table.find(consts, "wait") then

            print("FOUND SHOOT FUNC", v)

            for i, c in pairs(consts) do
                print(i, c)

                if c == 2 then
                    debug.setconstant(v, i, Value)
                end
            end
        end
    end
end
   end,
})
local function Apply()
    for _, v in pairs(getgc(true)) do
        if type(v) == "table"
        and rawget(v, "AddVelocity") then

            hookfunction(v.AddVelocity, function(...)
                return
            end)

            print("Blocked recoil spring", v)
        end
    end
end
local Button = Tab:CreateButton({
   Name = "Remove recoil",
   Callback = function()
   Apply()
   end,
})
local Section = Tab:CreateSection("Aim")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local selectedPlayer = nil
local connection = nil
local Aimbotstarted = false

local function AimbotStart()
    if Aimbotstarted then return end
    Aimbotstarted = true

    local camera = workspace.CurrentCamera

    connection = RunService.RenderStepped:Connect(function()
        if not selectedPlayer then return end

        local targetPlayer = Players:FindFirstChild(selectedPlayer)
        if targetPlayer
        and targetPlayer.Character
        and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then

            camera.CFrame = CFrame.lookAt(
                camera.CFrame.Position,
                targetPlayer.Character.HumanoidRootPart.Position
            )
        end
    end)
end

local function AimbotStop()
    Aimbotstarted = false

    if connection then
        connection:Disconnect()
        connection = nil
    end
end

local Dropdown = Tab:CreateDropdown({
   Name = "Select player",
   Options = getPlayerList(),
   CurrentOption = {},
   MultipleOptions = false,
   Flag = "PlayerList",

   Callback = function(Options)
       selectedPlayer = Options[1]
   end,
})

Players.PlayerAdded:Connect(function()
    Dropdown:Refresh(getPlayerList())
end)

Players.PlayerRemoving:Connect(function()
    Dropdown:Refresh(getPlayerList())
end)
local Toggle = Tab:CreateToggle({
   Name = "Aimbot",
   CurrentValue = false,
   Flag = "Aimbot", -- A flag is the identifier for the configuration file; make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
        if Value then
            AimbotStart()
        else
            AimbotStop()
        end
    end,
})
local Tab = Window:CreateTab("Hider", 4483362458) -- Title, Image
local Section = Tab:CreateSection("Teleport")
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local teleportEnabled = false

local function getHRP()
    local char = player.Character
    if not char then return nil end
    return char:FindFirstChild("HumanoidRootPart")
end

local Toggle = Tab:CreateToggle({
   Name = "Teleport to safe area",
   CurrentValue = false,
   Flag = "LoopTeleSpawn",
   Callback = function(Value)
       teleportEnabled = Value
   end,
})

task.spawn(function()
    while true do
        if teleportEnabled then
            local hrp = getHRP()
            local spawn = workspace:FindFirstChild("Lobby")
                and workspace.Lobby:FindFirstChild("SpawnLocation")

            if hrp and spawn then
                hrp.CFrame = spawn.CFrame + Vector3.new(0, 3, 0)
            end
        end

        task.wait() -- teleport speed
    end
end)
local Tab = Window:CreateTab("Buying", 4483362458) -- Title, Image
local Section = Tab:CreateSection("Crate")
local selectedCrate = nil

local Dropdown = Tab:CreateDropdown({
   Name = "Select Crate",
   Options = {"Elemental Crate", "Common Crate"},
   CurrentOption = {},
   MultipleOptions = false,
   Flag = "CrateDropdown",
   Callback = function(Options)
       local choice = Options[1]

       if choice == "Elemental Crate" then
           selectedCrate = "Elemental_1"
       elseif choice == "Common Crate" then
           selectedCrate = "Common_1"
       end
   end,
})
local Button = Tab:CreateButton({
   Name = "Buy crate",
   Callback = function()
   local args = {
	    selectedCrate
    }
    game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_net@0.2.0"):WaitForChild("net"):WaitForChild("RE/BuyCrate"):FireServer(unpack(args))
   end,
})
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local remote = ReplicatedStorage
    :WaitForChild("Packages")
    :WaitForChild("_Index")
    :WaitForChild("sleitnick_net@0.2.0")
    :WaitForChild("net")
    :WaitForChild("RE/BuyCrate")

local toggleEnabled = false

local Toggle = Tab:CreateToggle({
   Name = "Auto Open Selected Crate",
   CurrentValue = false,
   Flag = "AutoSelectedCrate",
   Callback = function(Value)
       toggleEnabled = Value
   end,
})

task.spawn(function()
    while true do
        if toggleEnabled and selectedCrate then
            
            local args = {
                selectedCrate
            }
            remote:FireServer(unpack(args))
        end

        task.wait(1) -- delay 1s
    end
end)
local Section = Tab:CreateSection("Lucky spin")
local Button = Tab:CreateButton({
   Name = "Spin the lucky spin",
   Callback = function()
   game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_net@0.2.0"):WaitForChild("net"):WaitForChild("RF/DoSpin"):InvokeServer()
   end,
})
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local remote = ReplicatedStorage
    :WaitForChild("Packages")
    :WaitForChild("_Index")
    :WaitForChild("sleitnick_net@0.2.0")
    :WaitForChild("net")
    :WaitForChild("RF/DoSpin")

local autoSpin = false

local Toggle = Tab:CreateToggle({
   Name = "Toggle lucky spin",
   CurrentValue = false,
   Flag = "AutoLuckySpin",
   Callback = function(Value)
       autoSpin = Value
   end,
})

task.spawn(function()
    while true do
        if autoSpin then
            pcall(function()
                remote:InvokeServer()
            end)
        end

        task.wait(1) -- delay 1 giây
    end
end)
local Section = Tab:CreateSection("Buy boost")
local selectedSkillOption = nil

local Dropdown = Tab:CreateDropdown({
   Name = "Select skill to buy",
   Options = {"Clone", "Speed"},
   CurrentOption = {},
   MultipleOptions = false,
   Flag = "CloneSpeedDropdown",
   Callback = function(Options)
       selectedSkillOption = Options[1]

       local args = {
	        selectedSkillOption
       }
       game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_net@0.2.0"):WaitForChild("net"):WaitForChild("RE/BuyAbility"):FireServer(unpack(args))
   end,
})
local Tab = Window:CreateTab("In/Out match", 4483362458) -- Title, Image
local Section = Tab:CreateSection("Revive")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local remote = ReplicatedStorage
    :WaitForChild("Packages")
    :WaitForChild("_Index")
    :WaitForChild("sleitnick_net@0.2.0")
    :WaitForChild("net")
    :WaitForChild("RE/RequestRevive")

local autoRevive = false

local Toggle = Tab:CreateToggle({
   Name = "Auto Revive",
   CurrentValue = false,
   Flag = "AutoReviveToggle",
   Callback = function(Value)
       autoRevive = Value
   end,
})

task.spawn(function()
    while true do
        if autoRevive then
            pcall(function()
                remote:FireServer()
            end)
        end

        task.wait() -- delay 1 giây
    end
end)
local Section = Tab:CreateSection("Ability")
local selectedAbilityType = nil

local Dropdown = Tab:CreateDropdown({
   Name = "Change ability",
   Options = {"Speed", "Clone"},
   CurrentOption = {},
   MultipleOptions = false,
   Flag = "AbilityTypeDropdown",
   Callback = function(Options)
       selectedAbilityType = Options[1]

       local args = {
	        selectedAbilityType
       }
       game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_net@0.2.0"):WaitForChild("net"):WaitForChild("RE/InteractAbility"):FireServer(unpack(args))
   end,
})
