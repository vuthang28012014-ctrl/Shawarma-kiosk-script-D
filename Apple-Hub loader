local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Player = game:GetService("Players")
local Localplayer = Player.LocalPlayer
local executor = "Unknown"
local UserID = Localplayer.UserId
local check = false

if identifyexecutor then
    executor = identifyexecutor()
elseif getexecutorname then
    executor = getexecutorname()
end
local Hwid
local JobID
local function getHwid()
    Hwid = (gethwid and gethwid()) or (getfpscap and tostring(getfpscap())) or "UNKNOWN"
end
local function getJobID()
    JobID = game.JobId
end
getHwid()
getJobID()
local result
local succ, res = pcall(function()
    result = game:HttpGet('https://vexbin.lol/vexlytics/sdk/check_key?script_id=ba502b7c&key=' .. getgenv().vex_key .. '&hwid=' .. Hwid .. '&exec=' .. executor .. '&job=' .. JobID .. '&userid=' .. UserID)
end)
print(result)
if not succ then
    warn("Request failed:", result)
    return
end
pcall(function()
    local HttpService = game:GetService("HttpService")
    
    local json = result
    
    local data = HttpService:JSONDecode(json)
    
    local code = data.code
    local message = data.message
    if check == true then
        if code ~= "KEY_VALID" and message ~= "Success" then
            Localplayer:Kick("Bypass detected. Repeated attempts may result in a permanent ban!")
        end
    end
    if getgenv and not getgenv().vex_key then
        Localplayer:Kick("Bypass detected. Repeated attempts may result in a permanent ban!")
    end
end)
local Test = nil

local MarketplaceService = game:GetService("MarketplaceService")

local Scripts = {
    [128001665358186] = {
        Name = "Shawarma Kiosk",
        Url = 'https://raw.githubusercontent.com/vuthang28012014-ctrl/Shawarma-kiosk-script-D/refs/heads/main/Shawarma%20kiosk%20script%20made%20by%20vuthang6G'
    },

    [125700405216363] = {
        Name = "Camo or get sniped",
        Url = 'https://raw.githubusercontent.com/vuthang28012014-ctrl/Shawarma-kiosk-script-D/refs/heads/main/Camo%20or%20get%20sniped%20script%20made%20by%20vuthang6G'
    },

    [14486123914] = {
        Name = "Protect the president",
        Url = 'https://raw.githubusercontent.com/vuthang28012014-ctrl/Shawarma-kiosk-script-D/refs/heads/main/Protect%20the%20president%20script%20made%20by%20vuthang6G'
    },
    [79268393072444] = {
        Name = "Sell lemons",
        Url = 'https://raw.githubusercontent.com/vuthang28012014-ctrl/Shawarma-kiosk-script-D/refs/heads/main/Sell%20Lemons%20script%20made%20by%20VuThang6G'
    },
    [126194330110954] = {
        Name = "Chicago Life",
        Url = 'https://gate.sirius.menu/gateway?id=sid_04usu24bgzau'
    }
}

local Window = Rayfield:CreateWindow({
    Name = "Apple-Hub loader",
    ScriptID = "sid_yilnd0avx1ef",
})

Rayfield:Notify({
   Title = "Please wait for a moment!",
   Content = "Please wait while we are preparing things!",
   Duration = 6.5,
   Image = 132863234944847,
})

local gameName = "Unknown"

pcall(function()
    gameName = MarketplaceService:GetProductInfo(game.PlaceId).Name
end)

local Tab = Window:CreateTab("Info", 4483362458)

Tab:CreateLabel("PlaceId: "..game.PlaceId)
Tab:CreateLabel("Game name: "..gameName)

local supported = Scripts[game.PlaceId]

if supported then
    Tab:CreateSection("Game Status")
    Tab:CreateLabel("✅ Supported Game")
    Tab:CreateLabel("Script: "..supported.Name)

    task.wait(1)

    local ok, err = pcall(function()
        Rayfield:Destroy()
        loadstring(game:HttpGet(supported.Url))()
    end)

    if not ok then
        warn("Error loading script:", err)

        Rayfield:Notify({
            Title = "Script Error",
            Content = tostring(err),
            Duration = 6,
            Image = 4483362458,
        })
    end
else
    Tab:CreateSection("Game Status")
    Tab:CreateLabel("❌ Unsupported Game")

    Tab:CreateSection("Supported Games")

    for placeId, data in pairs(Scripts) do
        Tab:CreateLabel(data.Name.." | "..placeId)
    end

    warn("No script available for this game")

    Rayfield:Notify({
        Title = "Warning!",
        Content = "No script available for this game!",
        Duration = 6.5,
        Image = 4483362458,
    })
end
